<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Crud Operations with Ejs</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
  <div class="bg-zinc-900 h-full w-full p-10 text-white">
    <div class="mb-5">
      <a class="text-blue-600  text-xs" href="/read">Read User</a>
    </div>
    <h1 class="text-3xl font-medium  tracking-tighter mb-4">Update Users</h1>
    <form  action="/update/<%= user._id %>" method="post">
        <input class="px-3 py-2 bg-transparent outline-none rounded-md border-2 border-zinc-800" name="name" type="text" placeholder="Name" value="<%= user.name %>">
        <input class="px-3 py-2 bg-transparent outline-none rounded-md border-2 border-zinc-800" name="email" type="text" placeholder="Email" value="<%= user.email %>" >
        <input class="px-3 py-2 bg-transparent outline-none rounded-md border-2 border-zinc-800" name="image" type="text" placeholder="Image Url" value="<%= user.image %>" >
        <input class="px-3 py-2 bg-yellow-500 text-black rounded-md ml-3" type="submit" value="Update User">
    </form>
  </div>
</body>
</html>