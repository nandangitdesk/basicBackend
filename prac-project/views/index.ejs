<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="../public/stylesheets/">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-900 text-white">
        <div class="form p-10">
            <form action="/create" method="post">
                <input class="block px-5 py-2 bg-zinc-800 w-full rounded-md outline-none" type="text" placeholder="Title goes here..." name="title">
                <textarea  class="block px-5 py-2 bg-zinc-800 resize-none mt-2 w-full rounded-md outline-none" placeholder="Enter your task details" name="details"></textarea>
                <input class="mt-2 bg-blue-600 px-5 py-2 rounded-md" type="submit" value="Create Task">
            </form>
        </div>
        <div class="tasks p-10 flex gap-3 flex-wrap">
            <% if (files.length > 0) { %>

                <% files.forEach(function(val){ %>
                    <div class="task  min-w-72 rounded-md bg-zinc-800 px-3 py-4">
                        <h1 class="title text-white tracking-tighter text-3xl"> <%= val %></h1>
                    <div class="flex justify-between items-center mt-3">
                        <a class="text-blue-500  " href="/file/<%= val %>">Read more</a>
                        <a class="text-zinc-600" href="/edit/<%= val %>">edit filename</a>
                        <form action="/delete/<%= val %>" method="post">
                            <button type="submit" class="text-red-500 text-xs">Delete</button>
                        </form>
                    </div>
                    </div>
                <% }) %> 
            
            <% } else { %>
                <h3 class="text-zinc-600">No tasks yet.</h3>
            <% } %>
            


        </div>
    </div>

    <script src="../public/javascripts/"></script>
</body>
</html>