<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="w-full flex gap-3 justify-end px-10">
            <a class="px-3 py-2 bg-red-500 rounded-md text-sm inline-block mb-5" href="/logout">Logout</a>
            <a class="px-3 py-2 bg-yellow-500 rounded-md text-sm inline-block mb-5" href="/profile/upload">Set profile picture</a>
        </div>
    <div class="flex gap-3">
      <div class="h-10 w-10  rounded-md overflow-hidden">
        <img class="h-full w-full object-cover" src="/images/uploads/<%= user.profilepic %>" alt="">
      </div>
      <h1 class="text-3xl text-white ">
        <span class="font-light">Hello</span> <%= user.name %>👋🏻
      </h1>
    </div>
      <h5 class="my-5">You can create a new post.</h5>
      <form  action="/post" method="post">
        <textarea name="content" placeholder="What's on your mind ?" class="block resize-none bg-transparent border-2 border-zinc-800 outline-none w-1/3 rounded-md p-3"></textarea>
        <input class="px-3 py-2 bg-blue-500 rounded-md block mt-2  text-sm" type="submit" value="Create new post">
      </form> 
      <div class="posts mt-20">
        <h3 class="text-zinc-400">Your posts</h3>
        <div class="postcontainer mt-5 flex gap-4 flex-wrap">
  
            <% user.posts.reverse().forEach(function(post){ %>
             
                <div class="post bg-zinc-800 border-[1px] border-zinc-700 w-1/3 rounded-md p-4 ">
                    <h4 class="text-blue-500 mb-2 "><%=user.username%></h4>
                    <p class="tracking-tight text-sm"><%=post.content%></p>
                    <small class="text-xs mt-4 inline-block"><%= post.likes.length %> Likes</small>
                    <div class="btns flex gap-4 mt-1">
                        <a class="text-sm text-blue-500" href="/like/<%= post._id %>"><%= post.likes.indexOf(user._id) === -1 ? "Like" : "Unlike" %></a>
                        <a class="text-sm text-zinc-600" href="/edit/<%= post._id %>">edit</a>
                        <a class="text-sm text-zinc-600" href="/delete/<%= post._id %>">delete</a>
                    </div>

                </div>
           <% }) %>
             
   
        
           
        </div>
      </div>
   
    </div>
  </body>
</html>
