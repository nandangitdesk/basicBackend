<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Post</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="w-full flex justify-end px-10">
            <a class="px-3 py-2 bg-red-500 rounded-md text-sm inline-block mb-5" href="/logout">Logout</a>
        </div>
        <h5 class="mb-5">You can update your post.</h5>
        <form action="/update/<%= post._id %>" method="post">
            <textarea name="content" placeholder="What's on your mind ?" class="block resize-none bg-transparent border-2 border-zinc-800 outline-none w-1/3 rounded-md p-3"><%= post.content %></textarea>
            <input class="px-3 py-2 bg-yellow-500 rounded-md block mt-2 text-black text-sm" type="submit" value="Update post">
        </form>
    </div>
</body>
</html>
